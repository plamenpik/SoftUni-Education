using System;

class MagicExchangeableWords
{
    static void Main()
    {
        string[] words = Console.ReadLine().Split();

        string a = words[0];
        string b = words[1];

        Console.WriteLine(ExchangeableWords(a, b).ToString().ToLower());
    }

    static bool ExchangeableWords(string a, string b)
    {
        bool isExchangeable = false;



        return isExchangeable;
    }
}
